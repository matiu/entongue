extends layout


block content
    h1 
        img.img-responsive(src='/images/logomovil.jpg', alt='Entongue')
            
    div#themap
    
    .text-center
        a#entongar(class="btn btn-danger btn-large disabled", style="width:80%; margin:10px auto" ) ENTONGAR

    script.
        if (navigator.geolocation) 
            navigator.geolocation.getCurrentPosition(success);
        else
            alert('Geo Location is not supported');

